## 1. UI Foundation

- [x] 1.1 Add shared SwiftUI design tokens and reusable FE-style base components for surfaces, buttons, badges, and page containers
- [x] 1.2 Replace the default root tab chrome with a custom FE-aligned application shell and bottom navigation

## 2. Primary Flow Screens

- [x] 2.1 Rebuild the home, exercise selection, and guide screens to match FE layout while keeping existing exercise and quota data wiring
- [x] 2.2 Rebuild the analyzing and result screens to match FE status presentation while preserving the current analysis flow behavior

## 3. Supporting Screens

- [x] 3.1 Rebuild the history list and history detail presentation to align with FE filtering and card-based record display
- [x] 3.2 Rebuild the profile and paywall screens to align with FE visuals while keeping existing subscription actions available

## 4. Verification

- [x] 4.1 Verify the SwiftUI target compiles after the UI rewrite
- [x] 4.2 Mark the change complete in OpenSpec task tracking with any implementation notes needed for follow-up
