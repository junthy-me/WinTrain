## 1. 领域语义整理

- [x] 1.1 在 `docs/architecture/domain-models.md` 中定义核心领域对象
- [x] 1.2 区分分析会话、分析结果和历史记录的语义边界
- [x] 1.3 明确订阅状态与设备身份在领域层的角色

## 2. 实现映射确认

- [x] 2.1 对齐 iOS `AppModels.swift` 的客户端模型
- [x] 2.2 对齐后端分析服务中的结果对象语义
- [x] 2.3 确认该层不包含具体数据库字段定义

## 3. OpenSpec 工件补齐

- [x] 3.1 创建 `domain-models` change proposal
- [x] 3.2 创建 `domain-models` change design
- [x] 3.3 创建 `domain-models` delta spec
- [x] 3.4 创建 tasks 并回填当前状态
